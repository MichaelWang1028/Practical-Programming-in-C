#include <stdio.h>
#include "structs.h"


int main() {
  printf("Not sorting size is %lu bytes.\n", sizeof(foo));
  printf("Sorting from largest to smallest size is %lu bytes\n", sizeof(foo2));
  return 0;
}
